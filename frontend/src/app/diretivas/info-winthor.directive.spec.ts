import { InfoWinthorDirective } from './info-winthor.directive';

describe('InfoWinthorDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoWinthorDirective();
    expect(directive).toBeTruthy();
  });
});
