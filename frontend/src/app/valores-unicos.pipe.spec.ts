import { ValoresUnicosPipe } from './valores-unicos.pipe';

describe('ValoresUnicosPipe', () => {
  it('create an instance', () => {
    const pipe = new ValoresUnicosPipe();
    expect(pipe).toBeTruthy();
  });
});
